apiVersion: v1
kind: Service
metadata:
  name: {{ include "autotrim.fullname" . }}
  labels:
    {{- include "autotrim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autotrim.selectorLabels" . | nindent 4 }}
